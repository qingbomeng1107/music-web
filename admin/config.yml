backend:
  # 必须配置 git-gateway
  name: git-gateway
  branch: main # 您的生产分支名称

  # 必须添加完整的 Identity 地址
  identity_url: "https://visionary-brioche-501929.netlify.app/.netlify/identity" 
  gateway_url: "https://visionary-brioche-501929.netlify.app/.netlify/git"

# 必须添加 site_url
site_url: "https://visionary-brioche-501929.netlify.app"

# 媒体文件夹配置 (我们设置为标准路径)
media_folder: "assets/uploads" 
public_folder: "/assets/uploads" 

collections:
  # 这里开始是您的内容集合，请将下面的 collections 部分替换为您原文件中的内容。
  - name: "data"
    label: "数据管理"
    files:
      - file: "data.json"
        label: "歌曲分类与内容"
        # ... 以下部分与您原文件一致
